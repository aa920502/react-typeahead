import { MainWrapper } from "../style";
import { SearchInput } from "./SearchInput";

const Typeahead = () => {
    return (
        <MainWrapper>
            <SearchInput />
        </MainWrapper>
    );
};

export default Typeahead;
